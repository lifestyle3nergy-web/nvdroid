# Test programme

Required gates are locked installation, configuration validation, syntax compilation, unit tests, deterministic ordering, secret redaction and evidence generation. Integration environments add real Android network/battery observations, runner authentication, task timeout, cancellation, SIGTERM cleanup and artifact-digest verification.

Measure latency, jitter, loss, bytes, transport, metered state, battery, thermal state and execution cost. Do not infer metering from transport.

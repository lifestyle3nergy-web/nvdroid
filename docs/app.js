const target=document.querySelector("#capabilities");const response=await fetch("data/capabilities.json");if(!response.ok)throw new Error("Capability library unavailable");for(const item of await response.json()){const card=document.createElement("article");card.className="card";const title=document.createElement("h3");title.textContent=item.name;const text=document.createElement("p");text.textContent=item.description;card.append(title,text);target.append(card)}
